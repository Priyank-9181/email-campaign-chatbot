# Backend API base URL (no trailing slash).
# Copy this file to .env and change if your FastAPI server runs elsewhere.
# Restart the Vite dev server after editing .env.
VITE_API_URL=http://localhost:8000
